# Secret Detector

![Логотип Secret Detector](./assets/secret-detector.svg)

[🇬🇧 Read in English](README.md)

## Описание

Secret Detector — это быстрый инструмент на C++17 для поиска чувствительных данных в исходном коде: API‑ключей, токенов, паролей, URL баз данных, приватных ключей и других секретов.  
Поддерживаются как консольный сканер, так и GUI на Qt.

---

## Возможности

- Поиск распространённых типов секретов:
  - AWS access keys  
  - токены GitHub (classic и fine‑grained)  
  - приватные ключи (RSA, DSA, EC, OpenSSH)  
  - пароли в простых присваиваниях  
  - API‑ключи и URL‑ы подключения к БД  
  - JWT‑токены
- Рекурсивное сканирование директорий с учётом `.gitignore`
- Гибкие include/exclude‑фильтры (расширения и шаблоны путей)
- Уровни критичности: `CRITICAL`, `HIGH`, `MEDIUM`, `LOW`
- Настраиваемые правила детекции в `config/patterns.json`
- GUI:
  - интерактивная таблица находок с сортировкой и подсветкой по критичности
  - двойной клик открывает файл на точной строке в редакторе
  - настройка предпочитаемого редактора (VS Code, Vim и т.д.)
  - экспорт отчётов в Text / JSON / CSV / HTML
- CLI:
  - машинно‑читаемый вывод (JSON/CSV/HTML) для CI/CD
  - ненулевые коды возврата, удобные для пайплайнов и pre‑commit‑хуков

---

## Требования

### Зависимости для сборки

- Компилятор с поддержкой C++17 (GCC 11+, Clang 12+)
- CMake 3.16+
- Библиотеки:
  - `nlohmann-json3-dev`
  - `libpcre2-dev`
  - `libspdlog-dev`
- Для GUI:
  - `qtbase5-dev`
  - `qttools5-dev`
  - библиотеки Qt5 runtime (`libqt5widgets5`, `libqt5gui5` и др.)

Пример (Ubuntu):

```bash
sudo apt update
sudo apt install -y build-essential cmake git nlohmann-json3-dev libpcre2-dev libspdlog-dev qtbase5-dev qttools5-dev
```

---

## Сборка из исходников

```bash
git clone https://github.com/<your-username>/SecretDetector.git
cd SecretDetector
mkdir -p build
cd build
cmake -DCMAKE_BUILD_TYPE=Release ..
make -j"$(nproc)"
```

CLI+GUI по умолчанию собираются в каталоге `build`.

### Сборка только CLI (без Qt)

```bash
mkdir -p build-cli
cd build-cli
cmake -DCMAKE_BUILD_TYPE=Release -DBUILD_GUI=OFF ..
make -j"$(nproc)" secret_detector
```

---

## Конфигурация правил поиска

Все правила заданы в `config/patterns.json` (регулярные выражения + критичность + описание).
Конфиг можно расширять под свои нужды, добавляя новые шаблоны секретов.

---

## Запуск CLI

```bash
./secret_detector <path> [options]
```

Примеры:

```bash
# Сканировать текущую директорию рекурсивно
./secret_detector . --recursive

# Учитывать .gitignore и сохранить отчёт в JSON
./secret_detector . --recursive --respect-gitignore --output report.json --format json

# Сканировать конкретный проект
./secret_detector /path/to/project --recursive --include-ext cpp,h,py --exclude build,.git,node_modules
```

Основные опции:

- `--recursive` — рекурсивное сканирование  
- `--respect-gitignore` — игнорирование файлов из `.gitignore`  
- `--exclude` — шаблоны путей для пропуска  
- `--include-ext` — список расширений файлов  
- `--output` — путь к файлу отчёта  
- `--format` — формат отчёта (`text|json|csv|html`)  
- `--strict` — ненулевой код возврата, если найден хоть один секрет  
- `--threads` — число потоков (0 = авто)

Коды возврата:

- `0` — секретов не найдено  
- `1` — найдены секреты  
- `2+` — ошибка выполнения или конфигурации

---

## GUI

После сборки с поддержкой GUI:

```bash
cd build
./secret_detector_gui
```

Интерфейс позволяет запускать сканирование по каталогу, фильтровать результаты по критичности, открывать файлы в редакторе и экспортировать отчёты.

---

## Интеграция с CI/CD (GitHub Actions)

```yaml
name: Secret Scan

on: [push, pull_request]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure build
        run: |
          mkdir -p build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..

      - name: Build CLI
        run: |
          cd build
          make -j"$(nproc)" secret_detector

      - name: Run Secret Detector
        run: |
          cd build
          ./secret_detector .. --recursive --respect-gitignore --strict --format json --output secret_report.json
```

---

## Ограничения

- Используется подход на основе регулярных выражений (pattern-based).  
- Детектор высокой энтропии реализован, но по умолчанию выключен из-за большого числа ложных срабатываний.  
- Анализ полностью локальный, без сетевых запросов.

---

## Roadmap / идеи

- Опциональный детектор высокой энтропии с настройкой порогов  
- Языко‑зависимые эвристики для уменьшения FP  
- Готовые хуки для pre-commit  
- Больше преднастроенных паттернов (облака, CI‑токены, OAuth и т.п.)

---

## Вклад

PR‑ы и ишью приветствуются.  
Старайтесь делать изменения точечными и добавлять краткое описание и шаги для проверки.

---

## Лицензия

Проект распространяется под лицензией MIT.  
Текст лицензии — в файле `LICENSE`.
